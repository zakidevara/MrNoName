using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TileType { START, GOAL, PATH, BLOCKED }
public class AStar : MonoBehaviour
{
    private TileType tileType;

    // Update is called once per frame
    void Update()
    {
        
    }
}
